# AGENTS — タスク進行ルールとワークフロー（プロジェクト向け）

このファイルは、私（作業エージェント）とあなたがこのリポジトリで作業する際の運用ルールとタスク進行指針をまとめたものです。

## 基本ルール
- 1タスク = 1コミット：各作業単位は独立したコミットで記録します。
- 自動コミット：実装完了かつビルド・テストが通ったら、私がローカルで自動的にコミットします（`git push` は行いません）。
- **開発効率化**：開発中は `npm run test:quick` で高速反復。機能完成時だけ `npm run test:full` で全テスト実行。
- **サーバー再利用**：ユーザーが既に起動しているサーバーを優先利用。私は新規起動を避け、必要な場合のみユーザーに起動を依頼します。
- **ログ制御**：`.logs/` フォルダに記録。バグ調査時のみ手動で確認。
- 変更は小さく保つ：タスクはできるだけ小さく区切り、可逆性を高めます。

## タスクライフサイクル
1. 要件／仕様確認（このファイルまたは `context/*` を参照）
2. `manage_todo_list` にタスクを追加・更新（私が必ず行います）
3. 実装（必要なファイルを作成/編集）
4. **開発中**: ビルド → `npm run test:quick` スキップ（高速反復）
5. **機能完成時**: ビルド → `npm run test:full` で全テスト実行
6. テストが通ればローカルコミット（自動）
7. ユーザーに完了報告（差分・変更点の簡潔な要約）

## コミット方針（テンプレート）
- コミットメッセージフォーマット:
  - `<scope>: <短い説明>`（例: `pwa: add auto-reconnect hook`）
  - 長い説明は必要なら本文に追加

## ローカルコミット手順（私が行います）
```bash
git add -A
git commit -m "<scope>: <短い説明>"
```
- pushはユーザーの許可がある場合のみ実行します。

## テスト/ビルドコマンド（参考）
- 依存インストール: `npm install`
- ビルド: `npm run build`
- **開発起動（ユーザー操作）**:
  - `npm run dev:gateway` ← ユーザーが実行（既起動サーバーを使用）
  - `npm run dev:mobile` ← ユーザーが実行（既起動サーバーを使用）
  - 私は既起動サーバーを使用、新規起動が必要な場合はユーザーに依頼
- **ポート清掃（必要に応じて手動実行）**:
  - `npm run dev:clean` ← ポート競合時にユーザーが実行（gateway & vite プロセス終了）
  - その後 `npm run dev:gateway` や `npm run dev:mobile` で再起動
- **テスト簡略化**:
  - `npm run test:quick` ← 開発中（スキップ）
  - `npm run test:full` ← 機能完成・リリース前のみ
- ログ確認: `tail -f .logs/*.log` （バグ調査時のみ）

## 作業優先度と次アクション（現状）
- 候補タスク:
  1. PWAのトークン自動リフレッシュ実装
  2. mDNS自動検出の実装
  3. LAN実機テストの実行と記録

- ユーザー指示に従い優先タスクを開始します。開始前に私が `manage_todo_list` を更新します。

## 変更ログの外部化
- 設計関連は `context/` にあります。実装は `apps/` 配下にまとめます。

## 非許可事項（自動化の制限）
- 私は決して自動で `git push` やリモートへの公開は行いません。
- 重要なマージや公開（プルリク作成等）はユーザーの明示的許可が必要です。

---

この `AGENTS.md` を基準に作業を進めます。次にどのタスクを優先しますか？（番号で指示してください）
