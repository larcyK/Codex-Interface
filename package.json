{
  "name": "codex-interface",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/gateway",
    "apps/mobile-pwa"
  ],
  "scripts": {
    "dev:clean": "pkill -f 'node.*gateway|vite' 2>/dev/null || true; sleep 1",
    "dev:gateway": "npm --workspace apps/gateway run dev",
    "dev:mobile": "npm --workspace apps/mobile-pwa run dev",
    "build": "npm --workspace apps/gateway run build && npm --workspace apps/mobile-pwa run build",
    "test": "npm --workspace apps/gateway run test",
    "test:quick": "echo '⏭️  Dev mode: tests skipped for faster iteration'",
    "test:full": "npm run build && npm --workspace apps/gateway run test"
  }
}
